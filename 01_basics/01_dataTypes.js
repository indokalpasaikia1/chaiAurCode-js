"use strict";
// treat all JS code as newer version

//alert("hello");// we are using node JS, not browser.


let name = "indukalpa" // string dataType
let age = 21 //number dataType
let isLoggedIn = false // boolean dataType
// null dataType ,which is also a stand-alone value
// undefined dataType ie value is not assigned.
// symbol dataType, for uniqueness

// objects

console.log(typeof(undefined)) // we get 'undefined'
console.log(typeof(null)) // we get 'object'
